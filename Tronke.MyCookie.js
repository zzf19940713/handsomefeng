/** * Created by tronke on 2017/4/26. *//** *@method 设置cookie的方法 * @param {string} key * @param {string} value * @param {number} num * @param {string} secure * @return null; */function setCookie(key, value, num, secure) {    //定义一个变量，这个变量是一个 字符串    var tmpStr = "";    if (key) {        tmpStr = encodeURIComponent(key) + "=" + encodeURIComponent(value) + ";";    }    // 如果 num没有传值，num是一个 undefined =false    if (num) {        var date = new Date();        date.setDate(date.getDate() + parseInt(num));        tmpStr += "expires=" + date + ";";    }    if (secure) {        tmpStr += "secure;"    }    document.cookie = tmpStr;}//2.获取cookie/** *  获取所有的cookie * * @return {{}} */function getCookie() {    var obj = {};//定义一个空的对象，用于接受你转换的后的值    var temp = document.cookie;    //判断是是否 有多个cookie    if (temp.indexOf(';') != -1) {        //有多个值 uname=1122334455; _ga=GA1.1.1940844688.1491919753        var arr = temp.split(';');        for (var i = 0; i < arr.length; i++) {            var arr1 = arr[i].split('=');  //  uname=1122334455     uname , 1122334455           // .trim()去掉前后的空格            obj[arr1[0].trim()] = arr1[1];        }    } else {        //就一个  uname=1122334455        var arr = temp.split('=');        obj[arr[0]] = arr[1];    }    return obj;//最后把结果返回出去}/** *指定的 键获取cookie的值。 * @param  {string}key * @return {*} */function getCookieByKey(key) {   var obj=getCookie();    return obj[key]}